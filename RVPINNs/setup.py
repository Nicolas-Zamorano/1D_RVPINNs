from setuptools import setup, find_packages

setup(
    name='RVPINNs',
    version='0.1',
    description='a python libary to solve Diferential Equations with VPINNs.',
    author='Nicolás Zamorano',
    packages=find_packages(),
)